I was bored, so I added this. You can write your own default.jsonc, so that you can set custom paths to textures.

However, be careful; 

Everything gets overwritten; this isn't minecraft where all things you just don't set are kept from the vanilla. (at least for now)

See programer.jsonc for example.